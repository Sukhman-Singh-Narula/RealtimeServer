# Core FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
websockets==12.0
websocket-client==1.6.4
python-multipart==0.0.6

# Pydantic and configuration
pydantic==2.5.0
pydantic-settings==2.1.0

# Database
sqlalchemy==2.0.23
aiosqlite==0.19.0

# Cache (Redis - optional, will fallback to in-memory)
redis==5.0.1

# Firebase (optional - will use mock data if unavailable)
firebase-admin==6.2.0

# OpenAI
openai==1.3.7

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Environment and file handling
python-dotenv==1.0.0
aiofiles==23.2.1

# Audio processing - Windows compatible versions
# Note: These are optional and will fallback gracefully if installation fails
# numpy>=1.21.0
# scipy>=1.7.0

# Error handling and utilities
tenacity==8.2.3
asyncio-throttle==1.0.2
requests==2.31.0
